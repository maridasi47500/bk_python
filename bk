my_var="nixCraft"
if [ -z "$GEOCODERAPIKEY" ]
then
      echo "\$GEOCODERAPIKEY is NULL, computer runs source bash profile for u"
      source ~/.bash_profile
else
      echo "\$GEOCODERAPIKEY is NOT NULL"
fi

python script.py 8000 localhost 1
